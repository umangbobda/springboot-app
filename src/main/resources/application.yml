spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true  # Optional: to see SQL queries in logs
  config:
    use-legacy-processing: true

server:
  port: 8080
  include-debug-info: true
  error:
    include-message: always

aws:
  sqs:
    endpoint: http://sqs.us-east-1.localhost.localstack.cloud:4566
    queue:
      url: http://sqs.us-east-1.localhost.localstack.cloud:4566/000000000000/ai-job-queue
  region: us-east-1
